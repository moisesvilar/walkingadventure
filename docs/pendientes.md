# Pendientes

Índice de lo que queda por **decidir**. No sustituye a los documentos: las decisiones cerradas viven en `game-design/` y el relato de cada iteración en `docs/starting.md`.

**Aquí ya no se anotan tareas de implementación.** El camino es otro, y tiene cuatro pasos por delante:

1. ~~La arquitectura de la app~~ → **hecha**: `game-design/arquitectura.md` (React Native con Expo, generador en un paquete compartido que sigue corriendo en Node, y un proxy ciego que guarda las claves sin identificar a nadie).
2. ~~El diseño de las pantallas, al menos las principales, documentado en artefactos.~~ → **hecho** el 6-ago-2026: seis artefactos, cuarenta pantallas, ningún momento del bucle sin dibujar. Índice, resúmenes y enlaces en `docs/pantallas.md`.
3. ~~El PRD completo, a partir de `game-design/` y del paso anterior.~~ → **hecho** el 7-ago-2026: `docs/prd.md` (116 RF, 16 RNF, exclusiones y supuestos declarados) y `docs/checklist.md` (42 specs en 6 bloques). Relato en `docs/starting.md` (XX).
4. **Las tareas de implementación**, que salen del PRD y se ejecutan con el bucle `/somo-spec-fable` → `/somo-dev-fable` → `/somo-qa-dev-fable` → `/somo-qa-tester-fable`, repitiendo hasta tener las pruebas en verde y las tareas cerradas. El backlog ya existe: `docs/checklist.md`.

Meter tareas aquí duplicaría la lista del paso 4 y las dos se desincronizarían.

Qué se conserva de `app/` y qué no lo decide `game-design/arquitectura.md`: el generador se **porta a un paquete compartido** que sigue corriendo en Node para no perder los tests de determinismo, y se hacen **de cero** el render, las pantallas y la capa de datos. Portar no es copiar: la rejilla, los cupos por celda, la cobertura de escenas y el tramo personal cambian bastante ese código.

Un pendiente de diseño no se cierra con código sino con un documento en `game-design/` y su iteración en `docs/starting.md`; mientras ese documento no exista, implementar es inventárselo, y lo inventado sin querer se convierte en decisión al quedar escrito en el código.

## Lo que falta por decidir

**No queda ningún pendiente de diseño de primer nivel.** Los ocho que había el 5-ago-2026 están cerrados, cada uno con su documento. Lo que queda son flecos anotados al final de cada uno —treinta y pocos en total— que no bloquean nada: casi todos son contenido que se decide al escribirlo (los nombres de los tres escalones de rango, la lista de oficios, el tope de caras por sitio) o números que solo salen midiendo (el tamaño de celda en tramos, cuántos pasos dura el prólogo).

Tres sí conviene mirarlos antes de tocar lo que dependa de ellos: **el tamaño de la celda** (`alcance-del-mundo.md`), del que cuelga la rejilla entera; **qué cuenta como "moverse"** (`accesibilidad.md`), del que el 6-ago-2026 se cerró la mitad —el vehículo se aparta— y sigue abierta la bici y la silla eléctrica, que es donde arrastra el reloj del mundo; y **si el rango puede bajar** (`progresion.md`), que tiene propuesta escrita y sin ratificar.

**El paso 2 está cerrado** (6-ago-2026). Los seis artefactos están en `docs/pantallas.md` con enlace y resumen. Dejó catorce decisiones nuevas, tres pendientes cerrados —el 2 de `bucle-jugable.md`, el 1 de `arquitectura.md` y medio del 1 de `accesibilidad.md`— y un documento que no existía, `game-design/lenguaje.md`. Conviene tenerlo anotado porque es el argumento de que el paso no era un adorno: ninguna de esas decisiones habría salido leyendo los documentos.

**El paso 3 está cerrado** (7-ago-2026): `docs/prd.md` y `docs/checklist.md`. **Lo siguiente es el paso 4, ejecutar el checklist con el pipeline**, y a partir de ahí la columna `Estado` del checklist es de quien orquesta el bucle: nadie la toca a mano. El PRD dejó marcados quince huecos de cobertura en `docs/testing.md` (RF con ⚠ sin escenario) que conviene rellenar antes o durante sus specs.

**Y uno nuevo, que solo apareció al ejecutar los flujos en un dispositivo** (10-ago-2026): **qué hace el botón atrás del sistema en Android**. `docs/flujo.md` declara las vueltas del arranque y la de la ficha a la lista, y nada más; en «Lo que hay hoy» no hay camino declarado de vuelta a la portada. Medido en el emulador: pulsarlo allí **se lleva la app entera** y deja la raíz vacía. En Android el botón existe siempre, así que no decidir es decidir que salga del juego desde cualquier pantalla. No bloquea nada hoy —la prueba dejó de afirmar una transición que el diseño no promete—, pero es de las cosas que se notan el primer día de uso real.

**La capa de teselas de A1P4, ratificada la carencia como defecto por el dueño** (14-ago-2026, el día del estreno en su iPhone): la pantalla «Dónde generar el mapa del juego» enseña hoy una superficie lisa a propósito — `app/pantallas/mapa-real.jsx` declara en su cabecera que dibujar las calles reales necesita un módulo nativo que ninguna spec ha nombrado, y antes que fingir un mapa, la superficie dice que no está (§6h). El dueño lo vio en su aparato y lo dictó sin ambigüedad: **«la usuaria tiene que poder ver el mapa con las calles reales aquí» — hay que corregirlo**. La decisión de *que* haya calles está tomada; lo que queda por decidir es **con qué dependencia** (la capa de teselas: `react-native-maps`, una librería de teselas sobre Skia, o lo que la spec proponga), y esa dependencia se ratifica en el prompt de su fila antes de lanzar, como `expo-location` en la 48 y Health Connect en la 46. Nota de diseño que la spec no puede perder: A1P4 es **la única pantalla del juego donde se ven las calles tal cual** — la elección no sienta precedente para el resto del juego, que pinta el mapa de fantasía.

La fila 53 le puso raíz y le quitó un sospechoso, y las dos cosas dejan el pendiente donde estaba pero mejor descrito. **La raíz**: `app/App.js:234-250` suscribe la `BackHandler` **solo cuando hay una consulta abierta**, así que fuera de las cinco pantallas de A6 el botón no lo gobierna nadie y lo resuelve React Native finalizando la actividad — confirmado que se lleva la app en A1P1, A2P1 y A2P3, y que funciona donde el flujo sí declara vuelta. **Y el sospechoso que se cae**: se creía que el rojo de guardar una copia en A6P7 era este mismo pendiente por otra puerta, y se midió que no — la partida se borraba **sin pulsar atrás jamás**, con la hoja de compartir 25 s en pantalla, porque `Share.share` resuelve en Android al lanzar el chooser; el atrás lo consume el selector del sistema y la app vuelve viva con el mismo pid. Son dos cosas distintas: aquella se arregló en la fila 53 y esta sigue siendo **decisión de diseño sin tomar** — qué debe hacer el botón donde el flujo no declara vuelta.

**Dos más que destapó la fila 49 al recorrer el bucle** (12-ago-2026), y que nadie ha decidido en voz alta: **abandonar una aventura la quema para siempre** — cerrar a medias la mete en `cerradas` igual que terminarla, y la memoria de la lista filtra por plantilla, así que en un barrio de tres calles con pocas plantillas el catálogo se agota rápido; puede ser lo que `bucle-jugable.md` §4 quiere, pero no está dicho. Y **marcar «este sitio no pega» en un sitio del lazo que estás andando no te libra de él**: con la huella congelada ya no cambia la cadena —correcto—, pero el juego te sigue mandando allí; qué debería pasar no está decidido.

**Las decisiones que esperan al iPhone** (12-ago-2026) viven con su inventario en `docs/iphone.md`: HealthKit para la fuente de salud, App Attest para la atestación y el módulo de la Actividad en Vivo. Son dependencias nativas y se ratificarán en el prompt de su fila, como `expo-location` (48) y Health Connect (46); no se deciden antes porque decidir sin poder verificar es apuntar una intención.

**~~Y uno del cotejo de la fila 50~~** (12-ago-2026): **~~qué hace abrir una salida cuando el aparato no da posición puntual pero hay una última conocida fresca.~~** → **hecho** el 13-ago-2026 en la fila 53, y con un giro que hay que contar porque vale más que la respuesta: la pregunta estaba bien planteada y su premisa era falsa. **No faltaba frescura: faltaba encender el GPS.** `posicionPuntual()` pedía con precisión **equilibrada**, y con esa el sistema ni siquiera enciende el sensor — 30-32 s de espera para un «no», con el Event Log de `dumpsys location` sin registrar una sola petición; con precisión alta, el mismo aparato entrega en **5,0 s**. La causa llevaba desde el 11-ago escrita en el propio módulo, veinte líneas por encima del defecto, y dos cotejos independientes lo habían atribuido al proveedor frío. Lo decidido, que se sostiene igual una vez quitada la premisa: la puntual se pide con **precisión alta**; **una sola cota de frescura, 90 s, para cualquier fijo que ancle el punto de partida, venga por la puerta que venga** —porque la puntual ya aceptaba caché rancia sin decirlo: aperturas medidas con fijos de 90,2 s, 279,6 s y 643,3 s, y una que ancló con un fijo de 193,5 s y 100 m de distancia—; **respaldo a la última posición conocida con esa misma cota**, que es lo que la pregunta original pedía; y **re-anclaje con el primer fijo que repare de verdad**, que es lo que hace admisible una cota generosa. Lo que no cambia: sin ninguna posición dentro de la cota la salida no se abre y sigue diciendo por qué.

**Y uno de la fila 46** (13-ago-2026): **si la fila de contar los pasos de A6P6 basta como razón de permisos ante Google Play.** Health Connect obliga a declarar un destino para «¿por qué me pides esto?», y el dueño decidió que aterrizara en A6P6 en lugar de en una pantalla nueva: la fila y su línea de aviso ya *son* esa razón, escritas con las reglas de lenguaje. La fila las cableó y las verificó en el aparato con partida y sin ella. Lo que no se ha podido mirar es con ojos de revisor de Play, que además pide política de privacidad para datos de salud. Se decidió a propósito **no inventar texto legal** dentro de la fila; si la línea se queda corta, vuelve como decisión de producto y no como redacción de paso.

**Y uno de la fila 53, que salió de hacer la aritmética** (13-ago-2026): **el telón salta al pasar por delante de casa**. `packages/nucleo/partida/regreso.js:39` promete justo lo contrario —«el minuto es lo que distingue "he llegado" de "he pasado por delante"»— y los números no lo sostienen: cruzar en línea recta los 100 m de diámetro del radio de regreso dura **71 s** a paso de paseo (1,4 m/s), contra los **60 s** de permanencia que se exigen, así que un lazo que pase por delante del portal cierra la salida a mitad. A paso vivo (1,8 m/s) son 55,6 s y no salta, lo que es peor que un fallo limpio: el mismo recorrido cierra o no según lo deprisa que vayas ese día. Es decisión de diseño y no arreglo —¿se sube la permanencia, se estrecha el radio, o las dos?—, y cualquiera de las tres cambia cuándo se cierra una salida, que es la pieza más cara de tocar del bucle. **No se decide aquí.**

## Ideas sin cerrar

Vienen de `docs/ideas.md` y no son compromisos: falta decidir si entran.

- [ ] Imágenes generadas para núcleos de población ficticios.
- [ ] Retratos de NPC a partir de su descripción.
- [ ] Nombres para los ríos.
- [x] ~~Posibilidad de tener varias partidas (sobre todo si me voy de vacaciones y quiero generar otro mundo efímero que a los pocos días dejaré de lado)~~ → resuelto en `alcance-del-mundo.md`, y sin partidas separadas: **una partida, muchos mapas**. El destino de vacaciones es una celda que no toca con la tuya, tú viajas entera y allí vuelves a ser forastera; un mapa que no visitas no cuesta nada y sigue ahí si algún día vuelves.

## Cerrados recientemente

- [x] **El lenguaje del juego** (5-ago-2026) — `game-design/lenguaje.md`. El lenguaje es inclusivo y, donde el castellano obliga a elegir sin motivo, **el sesgo va hacia el femenino**: el personaje llega con el femenino puesto, el reparto de NPCs se equilibra por generación y el oficio no arrastra el estereotipo. Con una restricción que manda sobre el cómo — los textos se leen en voz alta, así que antes de desdoblar se reformula, y nada de -e ni de -x. Es sesgo en el reparto y en la duda, nunca censura del personaje.
- [x] **Renombrado: «comarca» pasa a ser «mapa»** (5-ago-2026) — en los trece documentos. Donde «comarca» significaba la gente de allí y no la unidad de mundo, se ha reescrito («para la gente de ese mapa», «la gente que te va conociendo»), porque ahí «mapa» no decía lo mismo.
- [x] **La arquitectura de la app** (5-ago-2026) — `game-design/arquitectura.md`. **React Native con Expo**, porque JavaScript en el cliente es lo que permite compartir el generador con los tests headless — y de ahí una regla dura: el núcleo determinista no puede importar nada de React Native o deja de correr en Node. El generador se **porta a un paquete compartido** y se refactoriza (la rejilla, los cupos por celda, la cobertura de escenas y el tramo personal lo cambian bastante); de cero van el render, las pantallas y los datos. Y se escribe la parte que faltaba: **sí hay servidor**, un proxy que guarda las claves de LLM, imágenes y Places, verifica con atestación de plataforma que la app es la app sin identificar a nadie, cachea solo imágenes por su prompt de ficción y no registra nada. Un proxy con claves no es un servidor con partidas dentro, y esa distinción hay que mantenerla.
- [x] **La partida guardada** (5-ago-2026) — `game-design/partida-guardada.md`, y con ella se cierra el bloque de diseño entero. **El mundo se congela completo**, porque el determinismo del generador es respecto a unos datos de OSM que cambian y la semilla sola daría otro mapa dentro de un año; a cambio, una salida entera se juega sin cobertura. Se guardan **estado y hechos**, con el estado como autoridad y el registro como auditoría. Y el respaldo va por la **copia del sistema** más un fichero exportable, que sustituye a la idea de exportar la semilla — al congelar el mundo, la semilla dejó de reproducir nada.
- [x] **Seguridad, privacidad y menores** (5-ago-2026) — `game-design/seguridad-privacidad.md`. **Del móvil no sale nada del jugador**: solo las coordenadas al generar el mundo, una vez, y prompts de los que se excluye todo dato real — así que el chiste del anclaje tendrá que salir de plantilla, porque el modelo no puede saber cómo se llama el bar. Permiso de ubicación solo **«mientras se usa»**, que resulta bastar para todo lo decidido porque los pasos de fondo se leen al abrir. El anclaje que no vale **lo descarta el jugador** con un gesto reversible que anota sin resembrar. Y el juego **no distingue a un menor**: lo que protege a un crío ya estaba puesto por otras razones y protege igual a todo el mundo.
- [x] **Alcance del mundo** (5-ago-2026) — `game-design/alcance-del-mundo.md`. **El mundo es del jugador**: la semilla lo lleva dentro, así que dos vecinos ven mundos distintos — se gana intimidad y se renuncia a la fantasía compartida, y la semilla pasa a ser el dato crítico de la partida. **Los mapas son celdas de una rejilla**, con lo que crecer deja de ser regenerar y el invariante de no resembrar pasa de promesa a propiedad de la forma; celda dimensionada en tramos del jugador y anclada a una coordenada redondeada, no a su portal. Y **una partida, muchos mapas**: viajas entera, el rango no viaja porque es por núcleo, así que lejos vuelves a ser forastera sin necesidad de una sola regla nueva.
- [x] **Capa de NPCs** (5-ago-2026) — `game-design/npcs.md`, que cierra el pendiente 2 de `quests.md`. **El NPC no consume anclaje: hereda el del sitio**, así que la capa sale gratis en el recurso escaso (enmienda a `parametros-mundo.md`). El reparto **crece con lo jugado** —cada cara que conoces la conoces por algo— con generación perezosa y determinista por semilla + sitio + puesto. Su memoria es corta y **fiel**: el testigo es la única fuente de verdad en un mundo deformado, y no corrige lo que se cuenta en el pueblo, así que puedes saber la verdad y seguir siendo famoso por una mentira. Las franjas son de la escena y no de la persona, sin simular vidas. Y nadie cambia por el tiempo: solo por tus actos, y lo roto se repara.
- [x] **Progresión y economía** (5-ago-2026) — `game-design/progresion.md`. **No hay niveles**: hay rango social por núcleo, sin números, que sube por lo que se cuenta de ti y no por kilómetros. El oro compra saber y favores —nunca distancia, nunca dinero real— y lo que compras es la versión deformada que tiene ese informante, así que el oro usa la propagación en vez de saltársela. El rango cambia el trato y el precio de la información, no el catálogo. Y los objetos son llaves que abren otra puerta al mismo beat, nunca un requisito: el primer mecanismo real de arcos largos, y el que rescata los hallazgos de micro-encuentro.
- [x] **El personaje** (5-ago-2026) — `game-design/personaje.md`. Un personaje que interpretas, con la regla que evita la disonancia: **identidad sí, cuerpo no** — nada de resistencia ni velocidad, porque el cuerpo es real y ya está medido por el tramo personal. Se eligen nombre y oficio y nada más; lo que el personaje **es** para la gente se gana, y el mote nace del rumor y no del hecho, por núcleo. El oficio filtra las aventuras y colorea los textos, con **afinidad declarada** (cada plantilla dice a qué oficios pertenece, y puede pertenecer a varios) y su factura escrita: pocos oficios y un catálogo de 20-30 plantillas. Y dos personas caminando juntas se resuelve por lo barato: una partida, dos cuerpos, y textos escritos para leerse en voz alta.
- [x] **El arranque: las primeras semanas** (5-ago-2026) — `game-design/arranque.md`, que cierra el pendiente 3 de `bucle-jugable.md`. El agujero se acotó primero: no son "las primeras semanas" sino los días 3 al 10, cuando la novedad ya pasó y todavía no has hecho nada que nadie pueda contar. Tres decisiones: el mundo llega con pasado —rumores ya circulando y deformados antes de que aparezcas—, lo que de paso adelanta el mejor truco del juego a la primera salida · la deformación se pone en escena y no se explica, solo durante el arranque · y el arranque termina, marcado una sola vez, cuando llegas a un núcleo y lo que allí se cuenta eres tú.
- [x] **Accesibilidad** (5-ago-2026) — `game-design/accesibilidad.md`. No es un modo, es la unidad de medida: el tramo deja de ser 2 km y pasa a ser lo que tú andas en media hora, así que todo el juego se redimensiona solo y nadie carga con una etiqueta. Cuatro decisiones: el tramo se declara y se corrige midiendo, en silencio · el filtro sobre el grafo evita y declara, sin borrar mundo · cada aviso viaja por dos capas y el par mezcla bolsillo con pantalla · el suelo es moverse y no andar, con el límite medido (~250 m de radio) y dicho antes de instalar. Hallazgo que ahorró la mitad del trabajo: es el mismo caso que el barrio de tres calles, ya resuelto en `bucle-jugable.md` §7.
- [x] **El bucle jugable** (5-ago-2026) — `game-design/bucle-jugable.md`. Siete decisiones: el mapa registra lo que sabes y no dónde estuviste (cuatro niveles) · el visor del anclaje ocurre en la calle, al llegar · no hay presupuesto, la quest declara su tamaño · volverse a casa a mitad se cierra en corto y no genera rumor · el relevo va hacia dentro siempre y hacia fuera de vez en cuando · tono cómico-cálido con dos reglas que lo hacen sostenible · el barrio de tres calles se ataca por tres sitios, con el estirón ofrecido y nunca impuesto.
- [x] **Diseño de la vida del mundo** (5-ago-2026) — decisiones 1, 3, 4 y sección 6 de `quests.md`: kilómetros como reloj, propagación por el árbol de calzadas, frontera árbitro/narrador y micro-encuentros como canal de entrega. Ver `docs/el-hacedor-de-mundos.md` para de dónde salió.
- [x] **Idioma de los nombres en todo el mundo, no solo en parajes** — resuelto de hecho: `build.js` resuelve `namesFor(localeFor(lat, lon))` una vez y `app/js/names/gl.js` implementa la interfaz completa. El pendiente estaba en `game-design/parajes.md` describiendo un problema que ya no existía.

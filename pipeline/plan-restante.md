# Plan de lo que falta, y cómo se orquesta

Documento de relevo de la orquestación (12-ago-2026, al día a 13-ago). Escrito para que una sesión orquestadora nueva arranque de aquí sin necesitar la conversación en la que nació. La memoria del proyecto es esto, la bitácora (`docs/starting.md`), el registro (`pipeline/decisiones-orquestador.md`, §1–§15) y el checklist — nunca una sesión.

## Estado al escribir esto

Actualizado al cierre de la fila 53 (14-ago-2026). `main` en `be67442` más el cierre, limpia y subida. **El proyecto entero en verde por primera vez**: última suite (cotejo de la 53, en worktree propio) con **PASS del runner, EXIT=0** — @nucleo **2960 · 2957 · 0 · 3** (`mirado: true` ambas, `completo: true`), @app **21 · 12 · 0 · 9**, cero rojos, y la columna de límite declarado en **9 sin tocar**, diciendo su verdad. Los tres rojos históricos, apagados por la raíz (§17: en los tres casos la raíz no era la que el registro creía — las enmiendas de §13b y §12b están en §17b y §17c). Las 53 filas del checklist en `done`. Lo que queda es lo de la mesa del dueño (§17d), las fichadas de abajo, y el iPhone.

## Las tareas, en orden

### 1 · ~~Fila 50 — «los cableados que faltan»~~ → **hecha** (12-ago-2026)

Cerrada y cotejada: los tres cableados entregados y verificados en `wa-pixel` (cola sembrada leída con `run-as`, A2P0 vista en Madrid a 500 km con el topónimo por la ruta ciega, «Marcarlo» pulsando en el centro), más lo que el encargo no sabía — el prólogo entero se tiraba, no solo la cola; A2P0 estaba rota por orden de hooks; y la primera versión del arreglo de A4P8 la habría dado por buena su propia prueba. A2P0 entró en el diseño (41 pantallas, 94 aristas). El relato en la entrada XXXIV de la bitácora y en §11–§13 del registro; los dos rojos nuevos de la suite **no son regresión** (§13b).

### 2 · ~~Fila 46 — el zurrón~~ → **hecha** (13-ago-2026)

Cerrada y cotejada: Health Connect ratificado en el prompt (el patrón funcionó otra vez), el zurrón recorrido en el aparato, huérfanas a cero y **B7 entero**. Costó lo que el encargo no sabía — el minSdk a 26 (primer cambio de suelo de aparatos), la primera inyección de código nativo propio, y `zurron.yaml` a límite declarado con una forma nueva («depende del mundo») decidida por el dueño contra un criterio de su propia spec. El cotejo se abortó una vez y se repitió limpio; la lección de raíz —**el árbol compartido es un recurso igual que el emulador**— y los tres fichados de mecanismo están en §14. El relato completo, en la entrada XXXV de la bitácora y en §14 del registro.

### 3 · ~~`escena.cara` — fila corta de núcleo~~ → **hecha** (13-ago-2026, fila 51 `beats-con-cara`)

Cerrada y cotejada: **0 → 69 beats con cara** en 63 de 103 aventuras, casteabilidad idéntica byte a byte, y el primer cotejo en worktree propio (§15b). La premisa era falsa por novena vez —los beats sobre humanos no faltaban: reventaban—, las cuatro decisiones del dueño llegaron con números medidos en copia parcheada antes de la spec (el patrón exportable de la fila), y la segunda entrega (la guarda de plugins de §14e) vino mejor de lo pedido. Lo que **nadie ha visto**: la cara en la pantalla de un teléfono — fichado para la primera fila con aparato, junto con el resto de §15d. Relato en la bitácora (XXXVI) y §15.

### 4 · ~~Fila 52 — apagar `BOOT_COMPLETED`~~ → **hecha** (13-ago-2026)

Cerrada y cotejada: **la primera batería de núcleo 100 % verde del proyecto** — 2915 · 2912 · **0** · 3, con el receptor conservando exactamente `NOTIFICATION_EVENT` y el permiso inerte declarado con su motivo. La décima premisa falsa fue del encargo (el criterio pedía quitar el permiso, y quitarlo reventaba la app): la fila lo midió antes de obedecer, que es el método pagando otra vez. Dejó la lección por-clase-vs-por-acción para el tercer receptor futuro, la trampa del prebuild que no corre (hermana del total que baja), `plugins-declarados` endurecida con `nombraAlMenos`, y **una decisión nueva del dueño fichada**: la segunda vía de despertar (FCM, inerte, medida en tres direcciones) que la propiedad ancha de `permisos.js` cubre y la guarda no. Relato en §16.

### 5 · ~~Fila 53 — la primera fila con aparato~~ → **hecha** (14-ago-2026): **el proyecto entero en verde**

Cerrada y cotejada con **PASS del runner por primera vez** (2960 · 2957 · 0 · 3 y @app 21 · 12 · 0 · 9, límite declarado en 9 sin tocar). Los tres rojos históricos, apagados por la raíz — que en los tres casos no era la que creíamos: la puntual pedía precisión equilibrada (no «proveedor frío»: §17b enmienda a §13b/§16, con la causa escrita desde el 11-ago a veinte líneas del defecto), la partida se borraba al abrir la hoja sin esperar a nadie, y el telón por regreso era **estructuralmente imposible** (§17c enmienda a §12b). Deja el GPS-bajo-demanda medido, el reloj en la capa de plataforma, la cota única de frescura, los dos gestos, FCM neutralizado con su guarda, y cuatro trampas nuevas en `CLAUDE.md`. Lo pendiente de decidir quedó en la mesa del dueño (§17d). Relato en la bitácora y §17.

### 5-bis · Lo que era la fila 53 (histórico)

Junta lo acumulado con la etiqueta «para la primera fila con aparato», que ya tiene masa crítica: **la decisión del proveedor frío** (`docs/pendientes.md`: ¿cae la apertura a la última posición conocida con cota de frescura? — ratificarla en el prompt apagaría los rojos de `en-marcha` y `telon`), **la medición de la sospecha de la 46** (si `empezar-de-nuevo-copia` y el botón atrás son el mismo pendiente, dos deudas se vuelven una), **ver la cara en A4P3 con el dedo** (nadie la ha visto, §15d) y **medir el visor** con su spread fichado. Si sale bien, `@app` queda sin rojos o con uno.

**El hilo del telón por regreso ya está corrido** (13-ago, quien orquesta, en worktree — script `sonda-telon-regreso.mjs`): la hipótesis de §12b **tiene pata y con números**. En el punto de partida `(0,0)`, `cadenciaDeMuestreo` devuelve `por-distancia` en **6 de los 8 mundos de referencia** (el geofence más cercano a 19–191 m del borde, radio 40 m) — quien está parado en casa no recibe fijos, la parada no acumula, **el telón por regreso no puede saltar**. En los 2 restantes sale `por-tiempo` **por accidente de trazado** (un geofence pisa el anclaje: −8 m y −21 m). Donde funciona, funciona por azar: la familia del «depende del mundo», en el diseño mismo. **Entra en la fila 53**, y trae su decisión de diseño: que el punto de partida cuente como sitio para la cadencia (tocaría `bucle-jugable` y la iteración de SPEC-032 que corresponda).

### 6 · Fichadas sin fila, a propósito

- **Causa raíz de la versión global** (§11e): `VERSION_FORMATO` es global a las ocho clases de documento; subirla por una invalida las demás. Decisión de esquema, mejor con calma.
- **El telón por regreso** (§12b): el servicio en primer plano se cae a mitad de salida. El hilo barato de la hipótesis lo corre quien orquesta al preparar la 53 (ver arriba). Quien persiga la caída en aparato: `adb logcat` filtrado por el task manager desde **antes** de abrir la salida.
- **El lector de recursos del visor**: sin dueño natural hasta decidir de dónde salen las ilustraciones. Mientras, toda llegada resuelve a ficha o a lo-que-se-cuenta, y `visor.yaml` sigue en la columna.
- **Decisiones de diseño sin tomar**, en `docs/pendientes.md`: abandonar quema la plantilla, y marcar un sitio del lazo en curso no te libra de él (el botón atrás pasa a medirse en la 53).
- **La sospecha del `typeof`** (§15d): guardas que dejan de medir cuando el dato cambia de forma; su grep sobre `test/nucleo/` sigue pendiente.
- **El salto a iPhone**, que el dueño quiere «más pronto que tarde» en cuanto Android esté verificado: el inventario vivo está en `docs/iphone.md` — lo preparado, las tres decisiones de dependencia que exigirá (HealthKit, App Attest, Actividad en Vivo), los rojos que lo esperan (`empezar-de-nuevo-copia`) y la tabla de procedimientos de aparato que hay que re-medir. Toda fila que fiche algo con forma de iOS lo anota allí; el paso cero es instalar Xcode, que esta máquina no tiene, y **no depende de nadie: se puede disparar en paralelo cuando el dueño quiera**.

## Cómo se orquesta (el método, destilado de seis filas)

1. **Una fila, una sesión nueva**, con prompt autocontenido en `docs/prompts/prompt-*.md` (commiteado y subido antes de lanzar). Y desde la 50, dicho sin sutilezas porque «cada rol en su contexto» no bastó: **el encargo ordena explícitamente lanzar cada fase (`wa-spec` → `wa-dev` → `wa-qa-dev` → `wa-qa-tester`) como subagente con su propio contexto**, con la ventana principal solo para encargos cortos, veredictos y decisiones — y esa orden tiene que viajar **dentro del prompt que pega el dueño**, porque una sesión puede tener prohibida la herramienta de agentes salvo petición directa suya y un mensaje de quien orquesta no lo es (§13c). Si una sesión ya corre sin subagentes: volcados a disco y grep, nunca en la ventana.
2. **En los encargos, lo medido y lo heredado van etiquetados distinto** (§10-bis). Un negativo no se hereda: se mide de cero o se marca como sospecha. Y la sesión receptora verifica la premisa antes de usarla — seis de seis encontraron algo falso en la suya, y fue siempre lo más valioso.
3. **Una pregunta viva a la vez, en la ventana de quien ejecuta** (§11d). Si quien orquesta necesita una decisión del dueño sobre una fila, se la manda a la sesión para que la pregunte ella. Nunca un AskUserQuestion en paralelo: ya produjo dos respuestas contradictorias y una parada.
4. **Las decisiones del dueño se dan en la ventana de la sesión, nunca por relevo.** Un relato fiel de una decisión no es la decisión (§9b). Las condiciones de trabajo que acompañan sí pueden ir por mensaje.
5. **El emulador tiene un solo dueño a la vez** (§10e) — **y el árbol compartido es un recurso igual que el emulador** (§14b): un `node --test` sobre el árbol es una toma, y «avisar del fichero» no cubre «voy a ejecutar». Quien orquesta no lanza la suite mientras una fila lo tiene, el cotejo espera al cierre, y rigen dos reglas por decisión del dueño (§14e): `pgrep` contra el patrón (no memoria de lo lanzado, y cubre los subagentes en segundo plano), y ante un proceso ajeno posible, **preguntar antes de matar**. Los tres mecanismos quedaron decididos el 13-ago: cotejo en worktree propio (punto 7), protocolo escrito sin herramienta, y guarda de lista cerrada para `app/plugins/` — esta última la escribe `wa-qa-dev` en el próximo encargo.
6. **Quien orquesta no commitea mientras una fila tiene el checkout** — el árbol es compartido. Si es urgente, commit en la rama que esté y empuje por hash a main (`git push origin <hash>:main`).
7. **Al cierre de cada fila: cotejo independiente** — correr la suite entera, comparar contra los números declarados, y solo entonces dar por buena la fila (el merge lo suele hacer la sesión). Y desde la 46, **la parte de árbol del cotejo corre en un worktree propio** del hash declarado (`git worktree add`, la suite allí, borrar al terminar): decidido en §14e tras el cotejo que hubo que repetir. El emulador sigue siendo el único recurso con turno; la precondición de aparato limpio no cambia.
8. **Antes de cerrar una sesión, la pregunta de despedida**: «¿queda algo en tu cabeza que no esté en el repo?» — callejones probados, sospechas etiquetadas, terreno ajeno que chirrió, operativa, costuras con lo que venga. Ha producido §8, §10 y §12; lo verificable se verifica antes de escribirlo.
9. **El código de salida del runner nunca se lee a través de una tubería**, la etiqueta solo admite `SPEC-NNN`, `SPEC-NNN-iter-M` o `SUITE`, y un exit 2 no es verde. Y desde la 51: **una tanda sin aparato se invoca con `--nucleo-only`**, porque el runner toma el emulador por presencia (Maestro + `adb devices`), no por declaración — la invocación cómoda de este mismo punto se lo habría llevado. Las trampas del aparato (JDK 17, `exec-out`, `expo run:android` abre la app sola, nodos degenerados, `timeout` no existe en macOS) están en `CLAUDE.md`.
10. **La cuenta de `gh` se desconfigura sola** (algo la cambia a `moisesvvi`): si el push da 403, `gh auth switch --user moisesvilar && gh auth setup-git`, y avisar al dueño de cuál queda activa.
11. **Ante un requisito de compilación, mirar primero qué sabe hacer `app/plugins/`** (§14, lección de la 46): `expo-build-properties` estuvo a punto de entrar como «único camino declarado por la librería» y la salida estaba a un grep — `withGradleProperties` viene dentro de `expo` y el repo ya escribe plugins propios. La regla de pararse ante una dependencia funcionó; esta línea existe para que ni haga falta.

## El termómetro

- Columna de límite declarado: **9**, y el 8 → 9 es honesto: `zurron.yaml` entró por la forma nueva «depende del mundo» (recorre entero, 168 comandos; su verde depende de qué produjo aquel mundo), decidida por el dueño. La lista exacta y las **dos formas** viven en `test/nucleo/limite-declarado.test.mjs`, con la estricta por defecto y la laxa nombrándose a mano. Bajarla sigue siendo la medida de progreso real.
- Pantallas huérfanas: **0** (primera vez desde que existe la guarda). `BLOQUES_SIN_CONSUMIDOR`: **vacía**. Contratos sin llamador: **2**. Plugins nativos: **2, con guarda de lista cerrada** (huella SHA-256 sin comentarios) desde la 51. Mapa de cobertura: **3758 entradas válidas, 26 huecos de verdad con motivo** (8 heredados de SPEC-010/014/034/049).
- Rojos: **0 de núcleo desde la fila 52** — la primera batería 100 % verde (2915 · 2912 · 0 · 3) — + 3 de `@app`, todos ajenos y leídos: `empezar-de-nuevo-copia` (se creía de iOS, se reproduce en Android; quizá el mismo pendiente que el botón atrás, §14c), y `en-marcha` + `telon` (proveedor frío, §13b; su decisión de producto en `docs/pendientes.md`). Un rojo nuevo solo se acepta nombrando defecto, dueño y fila.
- Decisiones del dueño en cola para la 53 y aledaños: el proveedor frío, el punto de partida como sitio de la cadencia (§ tarea 5), y **la vía de despertar por FCM** (§16c: ampliar la propiedad «con la app cerrada» más allá del arranque — forma candidata, lista cerrada de vías de despertar).
- Y el patrón §6h sumó con la 50 sus apariciones más profundas hasta ahora —el prólogo entero producido y tirado, `levanta()` sin correr prólogo, una pantalla rota que nunca pudo protestar porque nadie llegaba— y con la 46 la variante nueva: **la fila del interruptor inerte fuera del `Switch`**, un control que se pinta y no responde. Las guardas que lo cazan —pantallas huérfanas, contratos sin llamador, piezas sin consumidor, límite declarado, manifiesto generado, aristas del flujo— son el mejor activo del repo después del propio juego.
